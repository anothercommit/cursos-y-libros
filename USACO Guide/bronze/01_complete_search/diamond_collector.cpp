#include <algorithm>
#include <bits/stdc++.h>

using namespace std;

int main() {
    int diamonds[] = {1, 6, 4, 3, 1};
    int len = 5;

    sort(diamonds, diamonds + len);

    int result = 0;

    for (int i = 0; i < len; ++i) {
        diamonds[i]
    }
}
